wb_test = predict(linclass, waterbottle_m)
k_test = predict(linclass, keys_m)
w_test = predict(linclass, wallet_m)
tb_test = predict(linclass, toothbrush_m)
us_test = predict(linclass, unscrewing_m)

